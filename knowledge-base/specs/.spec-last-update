# Spec Manager Last Update
commit: 34ef702
timestamp: 2026-08-24T15:50:00Z
specs_updated: 1
specs_created: 0

# Advanced to 34ef702 (the SEC-026 containment fix). SPEC-011 owns the refusal
# that fix restored and now records that it was absent on Python 3.9-3.11;
# SPEC-001's statement about the launcher's second body and its parity test
# still holds unchanged. An earlier version of this note claimed `specs_updated:
# 0` as "a checked zero" — that was a reasoning check, not a corpus pass, and
# the pass found one spec to update.
#
# What this commit claims, and what it does not.
#
# CLAIMED: every EXISTING spec is synced against the code as of 6312abe. All 30
# were loaded and mapped to the change set; 27 were affected; 9 had already been
# updated earlier on this branch; 11 were updated in this run (SPEC-001, 002,
# 004, 005, 007, 009, 011, 023, 025, 026, 030); the remaining 7 were checked
# function-by-function against the branch's diff and needed no change.
#
# NOT CLAIMED: spec-manager Phase 4 — generating specs for code no spec covers —
# was NOT run. 36 changed files are claimed by no spec. Two of them matter:
# `skills/freya-code-graph/scripts/containment.py` and `exec_path.py` are new
# shared modules that own the containment and program-resolution rules, and
# those rules are now documented across six specs but owned by none. Advancing
# this marker means the next `update` will not re-surface them, so they are
# named here instead of being left to be rediscovered.
#
# Previous value was 2deb4ef (the 0.3.0 merge — where the corpus was generated).
# The commit it was actually inferred at, 11cad13, is unreachable: it was
# squashed into 2deb4ef, so `git diff 11cad13..HEAD` fails in a fresh clone.
