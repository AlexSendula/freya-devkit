# Spec Manager Last Update
commit: 2deb4ef
timestamp: 2026-08-24T11:40:00Z
specs_updated: 0
specs_created: 0

# `2deb4ef` is the 0.3.0 merge — the reachable commit that carries the spec
# corpus as it was generated by the brownfield scan. It is NOT a claim that
# spec-manager has run since. It has not: this marker had never been written,
# so wrap-up's never-synced guard (F5) skipped Phase 3 on every run, which is
# why `specs_updated` and `specs_created` are 0 rather than a count.
#
# The commit the corpus was actually inferred at, `11cad13`, is unreachable —
# it was squashed into `2deb4ef` at the 0.3.0 merge, so a fresh clone does not
# have the object and `git diff 11cad13..HEAD` fails there. `2deb4ef` is the
# earliest reachable commit that contains the same tree, which makes the next
# Phase 3 diff a superset (it picks up a few docs-only commits) rather than a
# diff nobody else can reproduce.
#
# So the next `freya-spec-manager update` re-examines every code change from
# 0.3.0 to now, including all of `fix/security-findings-0-3-1`. That is the
# honest range: nothing between here and HEAD has been checked against the
# corpus by the tool.
